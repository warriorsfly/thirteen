
import 'package:flutter/cupertino.dart';

class AlbumModel extends ChangeNotifier {
  // AlbumDetail _detail;

  // AlbumDetail get detail => _detail;
  // set detail(AlbumDetail det) {
  //   _detail = det;
  //   notifyListeners();
  // }

  // Future<AlbumDetail> loadData(int id) async {
  //   var response = ;
  //   if (response.statusCode == HttpStatus.ok) {
  //     var json = jsonDecode(response.body);
  //     return AlbumDetail.fromJson(json);
  //   }
  // }
}
